export class IconPickerController {
  constructor($scope) {
    'ngInject';

    this.$scope = $scope;
  }
}

export class IconPickerPanelController {
  constructor($scope) {
    'ngInject';

    this.$scope = $scope;
  }
}
